# Replace double space with single space

name = "Shagufta  Jasim"

print(name.replace("  ", " ")) # Creating new string

# Strings are immutable which means that you cannot change them by running functions on them
